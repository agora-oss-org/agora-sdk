{
  "name": "@agora-sdk/monorepo",
  "version": "1.1.0",
  "license": "Apache-2.0",
  "author": "Agora SDK, maintained by Jenova Marie",
  "description": "Agora: Build interactive apps with social features like comments, votes, feeds, user lists, notifications, and more.",
  "keywords": [
    "agora",
    "replyke",
    "social",
    "comments",
    "reactions",
    "moderation",
    "reporting",
    "upvotes",
    "downvotes",
    "react",
    "react-native",
    "expo",
    "typescript"
  ],
  "bugs": {
    "url": "https://github.com/jenova-marie/agora-sdk/issues"
  },
  "homepage": "https://www.agora-oss.org",
  "repository": {
    "type": "git",
    "url": "https://github.com/jenova-marie/agora-sdk.git"
  },
  "private": false,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "major": "bash scripts/release.sh major",
    "minor": "bash scripts/release.sh minor",
    "patch": "bash scripts/release.sh patch",
    "version:patch": "pnpm --filter @agora-sdk/core --filter @agora-sdk/expo --filter @agora-sdk/react-js --filter @agora-sdk/react-native exec npm version patch --no-git-tag-version",
    "version:minor": "pnpm --filter @agora-sdk/core --filter @agora-sdk/expo --filter @agora-sdk/react-js --filter @agora-sdk/react-native exec npm version minor --no-git-tag-version",
    "build-all": "pnpm --filter @agora-sdk/core run build && pnpm --filter @agora-sdk/react-js run build && pnpm --filter @agora-sdk/react-native run build && pnpm --filter @agora-sdk/expo run build",
    "publish-prod": "pnpm --filter @agora-sdk/core --filter @agora-sdk/expo --filter @agora-sdk/react-js --filter @agora-sdk/react-native publish",
    "publish-beta": "pnpm --filter @agora-sdk/core --filter @agora-sdk/expo --filter @agora-sdk/react-js --filter @agora-sdk/react-native publish --tag beta",
    "publish-prod:patch": "pnpm version:patch && pnpm build-all && pnpm publish-prod",
    "publish-prod:minor": "pnpm version:minor && pnpm build-all && pnpm publish-prod",
    "publish-beta:patch": "pnpm version:patch && pnpm build-all && pnpm publish-beta",
    "publish-beta:minor": "pnpm version:minor && pnpm build-all && pnpm publish-beta"
  },
  "devDependencies": {
    "@types/node": "^20.14.5",
    "@types/react": "^18.0.0",
    "react": "^18.0.0",
    "react-native": "^0.79.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  }
}
