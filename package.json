{
  "name": "@agora/monorepo",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "author": "Replyke, maintained by Yanay Tsabary",
  "description": "Replyke: Build interactive apps with social features like comments, votes, feeds, user lists, notifications, and more.",
  "keywords": [
    "replyke",
    "social",
    "comments",
    "reactions",
    "moderation",
    "reporting",
    "upvotes",
    "downvotes",
    "react",
    "react-native",
    "expo",
    "typescript"
  ],
  "bugs": {
    "url": "https://discord.com/invite/REKxnCJzPz"
  },
  "homepage": "https://replyke.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/replyke/monorepo.git"
  },
  "private": false,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "version:patch": "pnpm --filter @agora/core --filter @agora/expo --filter @agora/react-js --filter @agora/react-native version patch --no-git-tag-version",
    "version:minor": "pnpm --filter @agora/core --filter @agora/expo --filter @agora/react-js --filter @agora/react-native version minor --no-git-tag-version",
    "build-all": "pnpm --filter @agora/core run build && pnpm --filter @agora/react-js run build && pnpm --filter @agora/react-native run build && pnpm --filter @agora/expo run build",
    "publish-prod": "pnpm --filter @agora/core --filter @agora/expo --filter @agora/react-js --filter @agora/react-native publish",
    "publish-beta": "pnpm --filter @agora/core --filter @agora/expo --filter @agora/react-js --filter @agora/react-native publish --tag beta",
    "publish-prod:patch": "pnpm version:patch && pnpm build-all && pnpm publish-prod",
    "publish-prod:minor": "pnpm version:minor && pnpm build-all && pnpm publish-prod",
    "publish-beta:patch": "pnpm version:patch && pnpm build-all && pnpm publish-beta",
    "publish-beta:minor": "pnpm version:minor && pnpm build-all && pnpm publish-beta"
  },
  "devDependencies": {
    "@types/node": "^20.14.5",
    "@types/react": "^18.0.0",
    "react": "^18.0.0",
    "react-native": "^0.79.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  }
}
